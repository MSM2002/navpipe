name: Publish to PyPI

on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"
  workflow_dispatch: # Allows manual testing from the Actions tab

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }} (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux 64-bit (Standard)
          - os: ubuntu-latest
            target: x86_64
          # Linux ARM (Using Native ARM runner to avoid QEMU/Ring errors)
          - os: ubuntu-24.04-arm
            target: aarch64
          # Windows 64-bit
          - os: windows-latest
            target: x64
          # macOS (Intel & Apple Silicon)
          - os: macos-latest
            target: x86_64
          - os: macos-latest
            target: aarch64

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          # We provide versions up to 3.13 for optimized native builds
          python-version: |
            3.8
            3.9
            3.10
            3.11
            3.12
            3.13

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          # Logic: Build optimized wheels for 3.8-3.13.
          # The --compatibility abi3 ensures a 'cp38-abi3' fallback is created for 3.14+.
          args: >
            --release
            --out dist
            --find-interpreter
            --interpreter python3.8 python3.9 python3.10 python3.11 python3.12 python3.13
            --compatibility abi3
          sccache: "true"
          manylinux: auto
        env:
          RUSTFLAGS: "-C opt-level=3"
          # Fix for 'ring' crate assembly on ARM/CI
          RING_PREGENERATE_ASM: "1"
          # Activates the [features] abi3 in your Cargo.toml
          MATURIN_FEATURES: "abi3"
          # Allows PyO3 0.22 to handle Python 3.14+ gracefully
          PYO3_USE_ABI3_FORWARD_COMPATIBILITY: "1"

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}-${{ matrix.target }}
          path: dist

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [build_wheels, sdist]
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist

      - name: Extract Release Notes from CHANGELOG
        id: extract_notes
        run: |
          VERSION="${{ github.ref_name }}"
          # Extracts content between the current version header and the next divider
          sed -n "/## \[$VERSION\]/,/---/p" CHANGELOG.md | sed '1d;$d' > RELEASE_BODY.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: RELEASE_BODY.md
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASES_TOKEN }}

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
